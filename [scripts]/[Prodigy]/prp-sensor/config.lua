Config = {}

Config.DigiScanner = `weapon_digiscanner`

Config.DefaultMinDistance = 30.0 -- This is the min distance that will be used if non is provided.

Config.DefaultDestinations = {
    --{
    --    id = "TEST",
    --    location = vec3(0.0, 0.0, 0.0),
    --    minDistance = 30.0,
    --}
}
