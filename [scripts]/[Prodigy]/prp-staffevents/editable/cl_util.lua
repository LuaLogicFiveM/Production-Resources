---@param state boolean
function SetKeybindsDisabled(state)
    if state then
        -- Here put code to disable keybinds registered by key mappings that interfere with staffevents.
    else
        -- Here put code to enable keybinds registered by key mappings that were interfering with staffevents.
    end
end